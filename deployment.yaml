apiVersion: apps/v1

kind: Deployment

metadata:
  name: widget-deployment

spec:
  replicas: 2

  selector:
    matchLabels:
      app: widget

  template:
    metadata:
      labels:
        app: widget

    spec:
      containers:
      - name: widget
        image: aayushpandey29/widget:v1
